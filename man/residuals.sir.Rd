% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir_methods.R
\name{residuals.sir}
\alias{residuals.sir}
\title{Extract Residuals from a SIR Model}
\usage{
\method{residuals}{sir}(object, type = c("deviance", "pearson", "response"), ...)
}
\arguments{
\item{object}{A fitted \code{sir} object from \code{\link{sir}}.}

\item{type}{Character string specifying residual type: \code{"deviance"}
(default), \code{"pearson"}, or \code{"response"}.}

\item{...}{Additional arguments (unused).}
}
\value{
An array with the same dimensions as \code{Y} containing the
  requested residuals. Contains NA where \code{Y} is missing. For a symmetric
  fit only the upper-triangle off-diagonal cells (the ones in the likelihood)
  are populated; the lower triangle and diagonal are NA, whereas
  \code{fitted()} returns the full symmetric matrix.
}
\description{
Returns residuals of the specified type. Response residuals are raw
(Y - fitted). Pearson residuals are standardized by the variance function.
Deviance residuals are signed square roots of the individual deviance
contributions and are most useful for diagnostic plots.
}
