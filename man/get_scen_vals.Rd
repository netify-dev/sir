% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{get_scen_vals}
\alias{get_scen_vals}
\title{Get Scenario Values for Prediction}
\usage{
get_scen_vals(
  data,
  vars = NULL,
  time = NULL,
  directed = TRUE,
  include_diag = FALSE,
  node_names = NULL,
  scen_vals = NULL
)
}
\arguments{
\item{data}{A 3D (\code{m x m x p}) or 4D (\code{m x m x p x T}) array of
influence covariates, or a data frame of covariates.}

\item{vars}{Character vector of variable names to compute scenario values
for. If NULL, uses all available variables.}

\item{time}{Optional time index for time-varying covariates. If NULL, values
are pooled across time.}

\item{directed}{Logical. If TRUE (default), treats the network as directed
when computing scenario values. For square arrays, diagonals are excluded
unless \code{include_diag = TRUE}; if FALSE, only the upper triangle is used.}

\item{include_diag}{Logical. If TRUE, include square-array diagonal cells when
computing quantiles. Keep the default FALSE for one-mode networks; use TRUE
for square bipartite sender/receiver arrays whose diagonal cells are real.}

\item{node_names}{Optional character vector of node names for labeling.}

\item{scen_vals}{Optional named list of pre-computed scenario values.}
}
\value{
A named list mapping each variable to a vector of quantile values.
}
\description{
Computes representative covariate values for model-implied scenario analysis.
For each influence covariate, returns a set of values (the 10th,
25th, 50th, 75th, and 90th percentiles) at which to evaluate predicted
influence.
}
\examples{
dat <- sim_sir(m = 8, T_len = 10, p = 2, q = 1, family = "poisson", seed = 1)
sv <- get_scen_vals(dat$W)
str(sv)
}
